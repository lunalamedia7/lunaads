name: test-curl4
on:
  workflow_dispatch: {}
jobs:
  tick:
    runs-on: ubuntu-latest
    env:
      CRON_SECRET: literaltoken123
    steps:
      - name: env-header
        run: curl -sf -X GET "https://lunaads.vercel.app/api/cron/process-publish-jobs" -H "Authorization: Bearer $CRON_SECRET"
